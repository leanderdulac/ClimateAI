# ClimateWise Production Requirements with Essential ML
# Production requirements including essential ML libraries (but not heavy TensorFlow)
# Use this for production services that need PyTorch but not TensorFlow

--extra-index-url https://download.pytorch.org/whl/cpu
# === FRAMEWORK WEB ===
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.27

# === CONFIGURAÇÃO E VALIDAÇÃO ===
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.2.2

# === BANCO DE DADOS ===
sqlalchemy==2.0.36
psycopg2-binary==2.9.11
asyncpg==0.29.0
aiosqlite==0.22.1
alembic==1.18.1

# === CACHE ===
redis==7.1.0

# === APIs EXTERNAS ===
requests==2.33.0
requests-cache==1.3.1
geopy==2.4.1
pycep-correios==5.2.0
openmeteo-requests==1.7.5
retry-requests==2.0.0

# === AUTENTICAÇÃO E SEGURANÇA ===
pyjwt[crypto]==2.12.1
passlib[bcrypt]==1.7.4

# === UTILITÁRIOS ===
pytz==2025.2
google-generativeai==0.8.6
httpx==0.28.1
psutil==7.2.2
email-validator==2.3.0  # Required by Pydantic's EmailStr
nolds==0.6.3  # Nonlinear dynamics analysis
tenacity==9.1.4  # Retry library with circuit breaker pattern
hvac==2.4.0  # HashiCorp Vault secrets manager
feedparser==6.0.12  # RSS/Atom feed parser for INMET alerts
beautifulsoup4==4.14.3  # HTML parser for INMET alert descriptions
aiohttp==3.13.5  # Async HTTP client for health checks
supabase>=2.3.0  # Supabase client for database integration

# === DADOS E ML ESSENCIAIS (PRODUÇÃO) ===
pandas==2.2.3
numpy>=1.26.0,<2.0.0  # Versão < 2.0 para compatibilidade com PyTorch
scikit-learn==1.5.2
scipy>=1.11.0,<1.14.0
statsmodels==0.14.6
torch==2.8.0+cpu  # PyTorch for LSTM attention mechanisms - CPU version explicitly specified
python-json-logger==4.0.0  # For structured JSON logging - Updated for security
# NOTA: TensorFlow NÃO incluído em produção (~500MB)

# ===== TAMANHO COMPARATIVO =====
# requirements-base.txt:     ~200MB
# requirements-prod-ml.txt:  ~700MB (PyTorch ~500MB)
# requirements-ml.txt:       ~1200MB (com TensorFlow)
# NOTA: Este arquivo deve ser usado para produção quando funcionalidades ML são necessárias
