# ClimateAI Production Requirements with Essential ML
# Production requirements including essential ML libraries (but not heavy TensorFlow)
# Use this for production services that need PyTorch but not TensorFlow

--extra-index-url https://download.pytorch.org/whl/cpu
# === FRAMEWORK WEB ===
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21

# === CONFIGURAÇÃO E VALIDAÇÃO ===
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# === BANCO DE DADOS ===
sqlalchemy==2.0.36
psycopg2-binary==2.9.11
asyncpg==0.29.0
aiosqlite==0.22.1
alembic==1.18.1

# === CACHE ===
redis==7.1.0

# === APIs EXTERNAS ===
requests==2.32.5
requests-cache
geopy==2.4.1
pycep-correios==5.2.0
openmeteo-requests
retry-requests

# === AUTENTICAÇÃO E SEGURANÇA ===
pyjwt[crypto]>=2.8.0
passlib[bcrypt]==1.7.4

# === UTILITÁRIOS ===
pytz==2025.2
google-generativeai>=0.4.0
httpx==0.28.1
psutil
email-validator>=2.0.0  # Required by Pydantic's EmailStr
nolds>=0.5.2  # Nonlinear dynamics analysis
tenacity>=9.0.0  # Retry library with circuit breaker pattern
hvac==2.3.0  # HashiCorp Vault secrets manager
feedparser>=6.0.0  # RSS/Atom feed parser for INMET alerts

# === DADOS E ML ESSENCIAIS (PRODUÇÃO) ===
pandas==2.2.3
numpy>=1.26.0,<2.0.0  # Versão < 2.0 para compatibilidade com PyTorch
scikit-learn==1.5.2
scipy>=1.11.0,<1.14.0
statsmodels>=0.14.0
torch==2.1.1+cpu  # PyTorch for LSTM attention mechanisms - CPU version explicitly specified
python-json-logger==4.0.0  # For structured JSON logging - Updated for security
# NOTA: TensorFlow NÃO incluído em produção (~500MB)

# ===== TAMANHO COMPARATIVO =====
# requirements-base.txt:     ~200MB
# requirements-prod-ml.txt:  ~700MB (PyTorch ~500MB)
# requirements-ml.txt:       ~1200MB (com TensorFlow)
# NOTA: Este arquivo deve ser usado para produção quando funcionalidades ML são necessárias
