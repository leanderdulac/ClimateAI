-- Database initialization for production
-- This file runs when the PostgreSQL container starts

-- Create extensions if needed
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create indexes for better performance (add as needed)
-- CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_table_column ON table_name(column_name);
